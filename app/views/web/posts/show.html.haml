%h2
  = @post.name

%pre= @post.text

%h3 Comments:
%ul
- @comments.each do |comment|
  %li
    =link_to comment.user.email, comment.user
    \:
    = comment.text
- if signed_in?
  = simple_form_for @new_comment, url: post_comments_path(@post.id) do |f|
    = f.input :text
    = f.submit :comment, class: 'btn btn-success'
by
= link_to @author.email, @author
